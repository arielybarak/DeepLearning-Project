# Models Directory

This directory stores the serialized model artifacts.

## File Types
- **`.h5` / `.keras`**: Saved TensorFlow/Keras models suitable for resumption of training or analysis.
- **`.tflite`**: Quantized models optimized for deployment on the ESP32-S3.

> **Note:** Binary model files are ignored by Git.
